#include <stdafx.h>
void swap(int * a, int *b);
int main()


{
	int a = 1, b = 2;
	printf("a=%d\n b=%d\n", b, a);
	swap(&a,&b);
	printf("a=%d\nb=%d\n", b, a);

	return 0;
}
void swap(int * a, int * b)
{
	int t = *a;
	*a = *b;
	*b = t;
	
}
