#include <stdafx.h> 
int isleapyear(int year) 
{
	return year % 4 == 0 && year % 100 != 0 || year % 400 == 0;
}

int main()
{
	int a;
	scanf_s("%d", &a);
	if (isleapyear(a) == 1)
	{
		printf("%d年是闰年\n", a);
	}
	else 
	{
		printf("%d年不是闰年\n", a);
	}


